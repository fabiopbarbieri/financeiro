import Input from '../Input';

const inputId: Input = {
  name: 'id',
  label: 'ID',
  type: 'number',
  readOnly: true,
  disabled: true,
  width: '50px',
  hidden: true,
};

export default inputId;
